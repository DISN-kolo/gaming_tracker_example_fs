# gaming\_tracker\_example\_fs
A fullstack project to learn Angular and .NET Core

## used:

To start a project quickly,

`cd back; docker run --rm -v $(pwd):/app -w /app mcr.microsoft.com/dotnet/sdk:10.0 dotnet new webapi -n Ggs.Back`
